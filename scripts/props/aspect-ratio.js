/*jslint browser: true, maxerr: 50, maxlen: 80 */

/*global define */

define(function () {
    'use strict';

    return Object.freeze({
        three_two: 0x01,
        sixteen_nine: 0x02,
        one_one: 0x04
    });
});
